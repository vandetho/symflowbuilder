import React from 'react';

export default function ImportFromUrl() {
    return (
        <div className="text-lg">
            <h1>Import from URL</h1>
            <p>
                This is a page where you can import a workflow from a URL. This is useful if you want to import a
                workflow from a different source.
            </p>
        </div>
    );
}
